
import React from "react"

const Badge = (props) => (
  <span style={{
    color: 'white',
    background: 'purple',
    padding: '10px',
    marginRight: '5px',
    marginBottom: '5px'
  }}>{props.text}</span>
)


export default Badge
